# nixpacks.toml — installs Tesseract OCR via apt (more reliable than nix packages)
[phases.setup]
aptPkgs = ["...", "tesseract-ocr", "tesseract-ocr-eng", "libgcc-s1"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn server:app --host 0.0.0.0 --port $PORT"
